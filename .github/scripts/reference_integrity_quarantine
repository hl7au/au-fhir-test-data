# Paths listed here are skipped by the reference integrity check.
# One path per line, relative to the repository root, e.g.
#   au-fhir-test-data-set/au-core/Condition-example.json
# Glob patterns work (*, ?, [abc]); a bare filename only matches a file at the root.
# Lines starting with # are comments; inline comments are not supported.
# Prefer fixing the reference over adding an entry here.
